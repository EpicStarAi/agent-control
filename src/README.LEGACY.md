# LEGACY — Vite client (frozen)

This `src/` directory (`App.jsx` / `main.jsx`, served by `vite:dev` / `vite:build`) is the
**old** EPIC GRAM web surface. It is **frozen** as of the P0 launch sprint (2026-06-26).

**Canonical web surface = `apps/web` (Next.js).**
All product scripts point there: `npm run dev` / `build` / `start` / `lint` → `next … apps/web`.

Do not add features here. Kept temporarily for reference only; remove once nothing depends on it.
