import { PortalLanding } from "@/components/PortalLanding";

// /services — переиспользует портал-грид с корня.
export const metadata = { title: "EPIC GRAM — Services" };

export default function ServicesPage() {
  return <PortalLanding />;
}
