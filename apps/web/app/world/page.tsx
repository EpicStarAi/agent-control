import { OpenClawWorldOS } from "@/components/OpenClawWorldOS";

// OPENCLAW_WORLD_OS_CORE_VISUAL_LAYER — standalone primary route (PHASE T12).
export default function WorldPage() {
  return <OpenClawWorldOS />;
}
