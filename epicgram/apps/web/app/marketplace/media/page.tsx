import { MediaStudio } from "@/components/MediaStudio";

export const metadata = { title: "EPIC GRAM — AI Media Creator" };

export default function MarketplaceMediaPage() {
  return <MediaStudio />;
}
