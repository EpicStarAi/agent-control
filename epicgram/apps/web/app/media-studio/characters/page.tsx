import { MediaCharacterWizard } from "@/components/MediaCharacterWizard";

export const metadata = { title: "EPIC GRAM — Character Builder" };

export default function CharactersPage() {
  return <MediaCharacterWizard />;
}
